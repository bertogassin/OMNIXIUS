# OMNIXIUS — local dev stack
version: "3.9"

services:
  api:
    build: ../backend-go
    ports:
      - "8080:8080"
    environment:
      - ENV=dev
    volumes:
      - ../backend-go:/app
  rust:
    build: ../services/rust
    ports:
      - "8081:8081"
  ai:
    build:
      context: ../ai
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
