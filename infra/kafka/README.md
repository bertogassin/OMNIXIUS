# Apache Kafka (OMNIXIUS)

For event streaming and real-time data at scale. Use when moving to high load.

- Topics (examples): `user.events`, `analytics.clicks`, `ai.requests`.
- Config and docker-compose add-on when needed.
