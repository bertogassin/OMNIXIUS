# Kubernetes (OMNIXIUS)

Placeholder for K8s manifests. Expand with Deployments, Services, Ingress, HPA.

```bash
kubectl apply -f namespace.yaml
# Later: deploy api, ai, rust as Deployments + Services
```
