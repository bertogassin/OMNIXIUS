# OMNIXIUS — Terraform root (placeholder)
terraform {
  required_version = ">= 1.0"
  # backend "s3" { ... }  # when using remote state
}

# Placeholder: add provider and resources (e.g. aws, kubernetes)
# module "cluster" { source = "./modules/cluster" }
