# OMNIXIUS — Android (Kotlin)

Нативное Android-приложение (ARCHITECTURE.md — мобилки). **Уровень:** заготовка; экран с названием. Сборка: Android Studio или Gradle (нужен JDK 17+ и Android SDK).

```bash
# При наличии gradlew в проекте:
./gradlew assembleDebug
# APK: app/build/outputs/apk/debug/
```
